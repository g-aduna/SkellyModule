pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = "skelly"

include(
":",
    ":common",
    ":gateway",
    ":client",
    ":designer"
)
